# Frontend

Estrutura base React + TypeScript + Vite organizada por features. O bootstrap atual existe somente para permitir a inicialização da aplicação; telas e regras de interface ainda não foram implementadas.
